import { Button } from '@/components/ui/button'
import React from 'react'

const Logout = () => {
  return (
    <Button variant="outline" className='w-fit px-20 '>Logout</Button>
  )
}

export default Logout